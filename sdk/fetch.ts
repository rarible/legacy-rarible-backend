// import { Response } from "node-fetch"

// export async function fetch(url: string, options?: any) : Promise<Response> {
//   if (typeof window === 'undefined') return import("node-fetch").then((f) => f(url, options))
//   else return (window.fetch(url, options) as unknown as Promise<Response>)
// }
