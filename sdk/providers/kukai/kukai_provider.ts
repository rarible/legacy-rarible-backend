import { TezosProvider, b58enc, hex_to_uint8array, edpk_prefix } from "../../common/base"
import { KukaiEmbed, Networks } from 'kukai-embed'


export async function beacon_provider(network: BeaconNetwork, name = "rarible") : Promise<TezosProvider> {

}
